export const colors = {
  primary: '#0f172a', // Deep Navy
  onPrimary: '#ffffff',
  secondary: '#0ea5e9', // Arctic Blue
  onSecondary: '#ffffff',
  tertiary: '#2dd4bf', // Electric Teal
  background: '#f8fafc',
  surface: '#ffffff',
  surfaceVariant: '#e2e8f0',
  error: '#ef4444', // Chilled Red
  warning: '#f59e0b', // Frost Amber
  success: '#2dd4bf', // Electric Teal for OK status
  textPrimary: '#0f172a',
  textSecondary: '#64748b',
};

export const typography = {
  fonts: {
    // Standard system fonts as fallbacks until custom fonts are loaded, assuming Inter is standard sans-serif
    sans: 'System', 
    mono: 'monospace',
  },
  sizes: {
    sm: 12,
    base: 14,
    md: 16,
    lg: 20,
    xl: 24,
    xxl: 32,
  },
};

export const spacing = {
  xs: 8,
  sm: 12,
  md: 16,
  lg: 24,
  xl: 32,
};

export const rounded = {
  sm: 4,
  md: 8,
  lg: 12,
  xl: 16,
  full: 9999,
};
